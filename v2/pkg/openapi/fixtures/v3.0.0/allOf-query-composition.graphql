schema {
    query: Query
}

type Query {
    pets: Pets
}

enum Breed {
    DINGO
    HUSKY
    RETRIEVER
    SHEPHERD
}

type Pets {
    address: String
    age: Int
    bark: Boolean
    breed: Breed
    color: String
    name: String
    pet_type: String!
    surname: String
}
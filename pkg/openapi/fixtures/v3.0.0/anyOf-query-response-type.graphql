schema {
    query: Query
}

type Query {
    petsFoobar(id: Int!): PetsFoobar
}

enum PetType {
    CAT
    DOG
}

type PetsFoobar {
    age: Int!
    hunts: Boolean
    nickname: String
    pet_type: PetType
}